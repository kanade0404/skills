# Cloud Delivery Pipeline

Fable が仕様を詰めて issue 群に落とし、Codex が実装し、CodeRabbit + Claude Code Actions がレビューする、クラウド完結の開発パイプラインの用語集。人間の明示ゲートは merge と差し戻し再検討の 2 点。

## Language

**タスク**:
Fable のタスク分解が出力する作業単位。outcome 単位の親 issue として表現され、AC・Non-goals・制約を持つ。実装単位ではない。完了 = 配下の全実装 issue の決着 + 親 AC の全満たし。
_Avoid_: チケット、サブタスク、TODO

**実装 issue**:
Codex がタスクを実装単位に分解して切る sub-issue。ちょうど 1 つの PR で完結する。分解の裁量は Codex にあり、Fable は関与しない。
_Avoid_: サブタスク、child ticket

**垂直スライス**:
実装 issue の粒度規準。単独で merge しても壊れない最小の振る舞い単位で、PR の実装差分がレビュー可能な規模 (目安〜数百行) に収まるもの。
_Avoid_: 水平分割 (レイヤ単位の分割)、WIP 分割

**AC (受け入れ条件)**:
タスクに付く、システム外から観測可能な振る舞いの記述 (入出力・状態変化・エラー時挙動)。実装語彙と検証手段を含まない — 検証手段の指定は実装 (テスト設計) への踏み込みだから。
_Avoid_: 完了条件 (DoD と混同)、テスト仕様

**Non-goals**:
タスクに付く「やらないこと」の明示。スコープの膨張 (ついで修正) と萎縮 (当然の省略) の両方を防ぐ。
_Avoid_: スコープ外 (文書によって意味が揺れるため)

**証明義務**:
Codex が PR で AC ごとの証拠 (テスト) を提示する義務。検証手段の妥当性はレビューが「AC ↔ 証拠の照合」で検証する。
_Avoid_: テスト義務 (手段を固定して聞こえるため)

**着手可**:
タスクの状態の一つ。blocked-by で張られた依存タスクが全て決着済みであること。Codex への投入は着手可になったタスクに対してのみ自動で行われる。
_Avoid_: ready (ラベル名と規約の混同を避けるため、状態名として使う)

**差し戻し**:
仕様の欠陥を理由にタスクを Fable の再検討へ返すこと。発生源は Codex (分解・実装時) とレビュー (PR 審査時) の両方で、基準は同一 — (1) AC の矛盾 (2) 制約 / Non-goals との抵触 (3) 垂直スライス不成立 (4) infeasible (AC は無矛盾だが技術的に実現不能)、の 4 つのみ。実装手段・内部設計・テスト設計の困難は差し戻し対象ではなく Codex の裁量内で、レビューのコード/実装レベル指摘は Codex が同 PR で修正する。

**決着**:
issue が closed になること。completed での close が正常決着。not_planned での close も決着だが、それに依存していた後続タスクには警告が付く。PR の merge されない close は実装 issue の決着ではない (失敗として人間へ)。
_Avoid_: 完了 (タスク完了 = 全実装 issue 決着 + 親 AC 全満たし、とは別概念)
_Avoid_: エスカレーション (人間宛てと混同するため)

**承認ゲート**:
人間が明示的に止めて判断する地点。このパイプラインでは merge と、差し戻し時の再検討 (通知を受けて人間が Fable session で行う) の 2 点。issue 起票は Fable との対話内の合意で代替し、Codex 着手は自動。仕様の書き換えは無人では行わない。
_Avoid_: レビュー (機械レビューと混同するため)

**Fable セッション**:
仕様・受け入れ条件の定義とタスク分解を行う claude.ai/code 上の cloud session。実装は行わない。
_Avoid_: ローカルセッション、メインセッション

**レビュー (機械レビュー)**:
CodeRabbit と Claude Code Actions による PR の自動レビュー。分担は観点別で排他ではない — セキュリティは shift-left の観点から**両系統が担う**。CodeRabbit は line-level のコード品質・バグ・セキュリティ。Claude Code Actions は仕様適合 (AC ↔ 証拠の照合)・アーキテクチャ・適合度関数・セキュリティ。
_Avoid_: レビュー単独 (承認ゲートと呼び分ける)

**適合度関数 (fitness function)**:
アーキテクチャ特性 (依存方向・レイヤ境界・モジュール結合・性能予算等) が保たれているかを機械的に判定する検査。Claude Code Actions のアーキテクチャレビューが評価する対象。
_Avoid_: アーキテクチャテスト (テストと混同するため)

## Flagged ambiguities

- **「レビュー」**: CodeRabbit / Claude Code Actions による機械レビューと、人間の merge 判断が混同されがち。前者は「レビュー」、後者は「承認ゲート」と呼び分ける。

## Example dialogue

> Dev: この機能、issue 1 本でいい?
> Expert: AC を見て。単独で merge して壊れないなら 1 タスク。DB migration と API 変更が別々に merge できないなら、それは 1 つの垂直スライス — 分けたら水平分割になる。
> Dev: 実装started のラベルは人間が付ける?
> Expert: 付けない。承認ゲートは merge だけ。Codex 着手は自動で始まる。
