# rules/

rulesync 配布元の **rules**（横断指示ルール）feature 枠。

- consumer は `rulesync fetch kanade0404/skills@<tag> --features rules` で取得し、
  `rulesync generate` で `CLAUDE.md` / `AGENTS.md` 等へ展開される。
- ここには **rulesync canonical 形式** の Markdown ルールを置く。
- 現状 placeholder（未収録）。移行は次フェーズ。
